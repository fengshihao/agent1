.venv/
logs/

# Local scratch / one-off captures (never commit)
.tmp_*
__pycache__/
*.pyc
*.pyo

# Python tooling caches (any subproject)
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# agent_core — Java library (standalone Gradle; also `java_agent :core` projectDir)
agent_core/.gradle/
agent_core/build/
agent_core/bin/

# java_agent — CLI + composite build
java_agent/.gradle/
java_agent/build/
java_agent/**/.gradle/
java_agent/**/build/
java_agent/**/bin/

# android_agent — Gradle / Android Studio outputs
android_agent/**/.gradle/
android_agent/**/build/
android_agent/**/local.properties

# 本地 clone 的微智 weizhi 工程（./sync-weizhi.sh），勿提交
/weizhi/

# weizhi publish 后导入的 Maven 产物（./import-weizhi-prebuilt.sh）；示例配置可提交
/android_agent/weizhi-prebuilt/maven/
/android_agent/weizhi-prebuilt/coordinates.properties

# Local memory notes
user_memory.md

# Java agent local SQLite long-term memory
.agent1/

# Editor configs
.vscode/
.cursor/
.idea/
